apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-golang-k8s-viewer.fullname" . }}-test-connection"
  labels:
    {{- include "chart-golang-k8s-viewer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-golang-k8s-viewer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
