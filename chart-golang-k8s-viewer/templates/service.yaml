apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-golang-k8s-viewer.fullname" . }}
  labels:
    {{- include "chart-golang-k8s-viewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-golang-k8s-viewer.selectorLabels" . | nindent 4 }}
